@model SaleManamentSystem.Models.ProductEntity

<div class="modal-header bg-warning text-dark">
    <h5 class="modal-title fw-bold">CẬP NHẬT SẢN PHẨM</h5>
    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
</div>

@using (Html.BeginForm("EditProduct", "Product", FormMethod.Post))
{
    <div class="modal-body">
        <div class="form-group mb-3">
            <label class="fw-bold">Mã sản phẩm</label>
            @Html.TextBoxFor(m => m.ProductID, new { @class = "form-control bg-light", @readonly = "readonly" })
        </div>

        <div class="form-group mb-3">
            <label class="fw-bold">Tên sản phẩm</label>
            @Html.TextBoxFor(m => m.ProductName, new { @class = "form-control" })
            @Html.ValidationMessageFor(m => m.ProductName, "", new { @class = "text-danger" })
        </div>

        <div class="form-group mb-3">
            <label class="fw-bold">Giá bán</label>
            @Html.TextBoxFor(m => m.Price, new { @class = "form-control", type = "number" })
            @Html.ValidationMessageFor(m => m.Price, "", new { @class = "text-danger" })
        </div>
    </div>
    <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Thoát</button>
        <button type="submit" class="btn btn-warning text-dark fw-bold">Lưu thay đổi</button>
    </div>
}
